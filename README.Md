# ViciousLayer
An Android system overlay utility designed for custom interface modifications.

## Project Structure
- `app/`: Main application module containing source code and resources.
- `.github/workflows/`: Automated CI/CD build configuration.

## Build Instructions
To build the project locally:
1. Ensure Java (OpenJDK 17) is installed.
2. Run the wrapper:
   ```bash
   ./gradlew assembleDebug
